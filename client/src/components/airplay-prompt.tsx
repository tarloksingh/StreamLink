import { Button } from "@/components/ui/button";
import { X, Tv } from "lucide-react";

interface AirPlayPromptProps {
  onDismiss: () => void;
}

export function AirPlayPrompt({ onDismiss }: AirPlayPromptProps) {
  return null; // Remove the popup completely
}
